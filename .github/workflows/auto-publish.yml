name: Auto Publish Generated Config

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Private build mode notice
        run: |
          echo "Auto publishing is disabled because private builds embed subscription nodes and credentials."
          echo "Do not publish output/shadowrocket.conf from private SUBSCRIBE_URL builds."
