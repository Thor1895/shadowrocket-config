# Generated by shadowrocket-config. Do not edit by hand.

[General]
bypass-system = {{ "true" if settings.general.bypass_system else "false" }}
skip-proxy = {{ settings.general.skip_proxy | join(", ") }}
dns-server = {{ settings.general.dns_server | join(", ") }}

[Proxy]
{% for node in nodes %}
{{ node.name }} = {{ node.type }}, {{ node.server }}, {{ node.port }}, encrypt-method={{ node.method }}, password={{ node.password }}
{% endfor %}

[Proxy Group]
{% for group in groups %}
{{ group.name }} = {{ group.type }}, {{ group.members | join(", ") }}
{% endfor %}

[Rule]
{% for section in rules %}
# {{ section.name }}
{% for item in section["items"] %}
{{ item }}
{% endfor %}
{% endfor %}
